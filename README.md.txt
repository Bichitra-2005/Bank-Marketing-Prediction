# Bank Marketing Response Prediction 🏦
A Machine Learning web app built with Streamlit to predict if a client will subscribe to a term deposit.

## 🚀 Features
- Predicts subscription based on 42 features.
- Interactive UI for data input.
- Uses a Random Forest/Logistic Regression model (mention yours here).

## 🛠️ How to run locally
1. Clone the repo.
2. Install requirements: `pip install -r requirements.txt`
3. Run: `streamlit run app.py`